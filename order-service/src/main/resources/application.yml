server:
  port: 8083
  servlet:
    context-path: /orders

spring:
  application:
    name: order-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/astracart_orders
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

# External service URLs
external:
  services:
    user-service: http://localhost:8081/auth
    product-service: http://localhost:8082/products

logging:
  level:
    com.astracart.order: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web.reactive.function.client: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

jwt:
  secret: myVerySecureSecretKeyForAstraCartApplicationThatShouldBeLongEnoughForHS256AlgorithmAndStoredSecurely
  access-token:
    expiration: 3600
  refresh-token:
    expiration: 604800
